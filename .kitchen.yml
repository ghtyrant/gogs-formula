---
driver: docker
driver_config:
  use_sudo: false

platforms:
  - name: centos-6.6
    driver_config:
      dockerfile: Dockerfile

provisioner:
  name: salt_solo
  formula: gogs
  state_top:
    base:
      '*':
        - gogs

suites:
  - name: default
